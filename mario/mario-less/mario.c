#include <stdio.h>
#include <cs50.h>

    int main(void)

    {
        int n;
        do
        {
           n = get_int("Size: ");
        }
         int i;{
            // Something happens
         }
            while (x > 1 && x < 8);

      while (n < 1);
      // For each row..
       for (int x = 0; x < n; x++)


